# config/nav-orphans.allow
#
# EN docs pages intentionally not linked from the sidebar (config/sidebar.mjs).
# Format: one slug per line with a trailing "# reason" comment (required).
# bin/check-orphans.mjs fails on unlisted orphans AND on stale entries here
# (slug now linked from the sidebar, or page deleted).

docs-overview  # legacy landing page, kept for inbound links
pricing  # superseded in the sidebar by pricing/compare-plans
translations/universal/brand-terms  # linked from the translations/universal index body, not the sidebar
translations/universal/password-passphrase  # linked from the translations/universal index body, not the sidebar
translations/universal/quality-checklist  # linked from the translations/universal index body, not the sidebar
translations/universal/secret-concept  # linked from the translations/universal index body, not the sidebar
translations/universal/voice-and-tone  # linked from the translations/universal index body, not the sidebar
